## Summary

<!-- What changed and why. Link the issue (e.g. "Closes #123"). -->

## Test plan

<!-- How you verified: commands run, cases covered. -->

- [ ] `npm run verify` passes
- [ ] `npm run test:coverage` passes (80% gate — local-only, not in CI)
- [ ] Sim changes: added/updated Vitest unit + deterministic-replay tests
